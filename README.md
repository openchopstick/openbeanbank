
  # Interactive Prototype for VATP

  This is a code bundle for Interactive Prototype for VATP. The original project is available at https://www.figma.com/design/dMqNHSCFImXQsymcLuxt1J/Interactive-Prototype-for-VATP.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  